import React from "react";

const Header = () => {
  return (
    <header className="border-b flex items-center justify-center py-6">
      <nav className="text-4xl font-semibold text-yellow-500 font-serif">
        Pablings Booking
      </nav>
    </header>
  );
};

export default Header;
